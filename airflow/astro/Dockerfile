FROM astrocrpublic.azurecr.io/runtime:3.2-5-python-3.14

# The load_people_api DAG's voter-data gate now runs in dbt Cloud (DbtCloudRunJobOperator), so the
# dbt project is no longer cloned into the image. The `loader` CLI is installed via requirements.txt.
