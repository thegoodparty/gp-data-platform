# Astro Runtime pre-installed provider packages: https://docs.astronomer.io/astro/runtime-image-architecture#provider-packages
duckdb==1.5.5
tabulate==0.10.0
pyarrow
numpy>=2.0.0,<3.0.0
pandas>=2.3.1,<3.0.0
paramiko>=5.0.0,<6.0.0
databricks-sql-connector>=4.3.0,<5.0.0
databricks-sdk>=0.117.0,<1.0.0
psycopg2-binary>=2.9.12,<3.0.0
sshtunnel>=0.4.0,<1.0.0
sqlparse>=0.6.0

# dbt steps run in dbt Cloud (DbtCloudRunJobOperator) rather than locally — dbt cannot run on the
# image's Python 3.14, and the full monorepo project needs ~24 parse-time env vars. This provider
# ships the operator + the dbt_cloud connection type.
apache-airflow-providers-dbt-cloud>=4.9.2,<5.0.0
# S3Hook for the l2_voter_files DAG. Astro Runtime pre-installs it; pinned so a runtime image
# that drops it fails the build rather than the DAG.
apache-airflow-providers-amazon>=9.16.0,<10.0.0
# KubernetesPodOperator for the container DAGs. Astro Runtime's slim image does not pre-install
# this one, so adding it here makes the next deploy a full image deploy rather than a DAG-only sync.
apache-airflow-providers-cncf-kubernetes>=10.21.1,<11.0.0
