# Dedicated venv for the Win outcomes data scout.
#
# Setup:
#   cd analytics/win_outcomes_scout
#   python3.12 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# IMPORTANT: databricks-connect must match your cluster's Databricks Runtime (DBR).
# Check via: Databricks UI -> Compute -> <cluster> -> "Databricks Runtime version"
#   (or `databricks clusters get --cluster-id <id> | jq .spark_version`)
# Common LTS pins:
#   DBR 14.3 LTS -> databricks-connect~=14.3.0
#   DBR 15.4 LTS -> databricks-connect~=15.4.0
#   DBR 16.x     -> databricks-connect~=16.0
# Do NOT install vanilla `pyspark` alongside — same namespace, will conflict.

databricks-connect~=15.4.0

# Analysis stack
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13

# Kernel registration (only needed if not using the Databricks VS Code extension)
ipykernel>=6.0
