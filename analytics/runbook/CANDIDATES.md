# Calibration candidates ledger

Last consolidation pass: **2026-08-17** (stale dashboard-breakage warnings in engagement.md + gotchas.md, approved item by item, landed in PR #839; previous 2026-07-21 cross-skill dedup + stale-pointer cleanup)

Shared and committed, unlike the personal, gitignored `CALIBRATION_*.md` logs. Decouples observing
something from proposing a doc edit: a run appends a line here instead of proposing an edit when an
observation is below the promotion bar, a data-state finding awaits confirmation, or a Track 2
process-design candidate is parked. Rows are never deleted; only the `status` column and the
last-consolidation date above are ever updated.

When to append, the promotion thresholds, and the branch/PR mechanics are owned by the calibration
step of the analytics-process skill
(`.claude/skills/analytics-process/references/calibration.md`) — this file does not restate them.

- **Entry format** (one table row per observation): date | track (`data`/`process`) | tag
  (`universal`/`data-state`) | one-line observation | run reference (branch or ticket) | status.

## Ledger

| date | track | tag | observation | run ref | status |
|---|---|---|---|---|---|
| 2026-06-11 | process | universal | analytics env lacks declared Databricks deps: `analytics/pyproject.toml` declares only pandas, but `databricks_conn.run_query` needs `databricks-sql-connector` + `databricks-sdk`, so every DB run requires `uv run --with ...`. Candidate: add a runtime dependency group. | diagnostics baseline arms 2026-06-11 (reps 1-2 + express-framing) | resolved (deps added in PR #636 / DATA-2143, 2026-07-21; no doc edit needed) |
| 2026-06-11 | process | data-state | `analytics/diagnostics/baseline_question.md` base numbers drifted: reproduced denominators +12-16/month (~0.15%) above the answer key, consistent with live-table drift or a day-boundary convention. Candidate: refresh the recorded base or pin the boundary explicitly. | diagnostics baseline arms 2026-06-11 (reps 1-2 + express-framing) | promoted (PR #610, calib/process/2026-07-15-brief-boundary-semantics; sighting 2026-06-11 + directed verification DATA-2114 meets the data-state bar per calibration.md — decomposition query confirmed both mechanisms: boundary reading 6-34/mo + live-mart drift ~0.1-0.3%) |
| 2026-07-15 | process | universal | `brief-schema.md` `target.definition` has no per-metric source field (only the population gets `source_model`); the R4 cold executor had to resolve `first_campaign_sent_at`'s home via the knowledge skill. It did so losslessly, so the layering worked as designed — promote only if metric-home ambiguity recurs as real friction. | DATA-2114 R4 arm (`analytics/diagnostics/logs/r4_brief_handoff_20260715.md`) | parked (Track 2 OFF) |
| 2026-07-15 | process | universal | Express path (DATA-2120 mechanism 4) scoped but deferred to its own PR. Settled design: folded single gate (express plan + one confirm); one-line close (or read-back rows if anything appended); non-negotiables kept = knowledge-skill resolution, live-catalog verification, boundary-semantics statement. Hardening required before shipping: falsifiable eligibility (any judgment call during concept resolution = fork = full pipeline), ineligible-request override recorded in the close line, "express — unreviewed" tag on the answer itself (not just the close), a to-do-seeding clause in `run-product-analysis.md` step 2, and a calibration.md carve-out so skipping the pass isn't a silent violation. Full design + risk notes in DATA-2120 comment (2026-07-15). | DATA-2120 scoping session 2026-07-15 | parked (deferred to own express PR) |
| 2026-07-15 | data | data-state | Serve `beyond_first_touch` (>=2 distinct event types, the lib default) is confounded by the two event generations: the >=2-types share fell ~93% (Jan–Mar) to ~48–77% (Apr–Jun) 2026 largely because the 2026 generation (Briefing Assistant / Community Issues / Org Switcher) fires fewer distinct types per session, not because depth dropped. Prefer >=2 distinct active days for cross-2026 depth comparison. | serve-monthly-engagement-depth ad_hoc 2026-07-15 | open |
| 2026-07-15 | data | data-state | First Serve cross-month return read: of a month's broad-engaged users only ~6–31% engage again the next month, decaying fast (Jan cohort 16→9→3→2%); post-Apr Ns small (~50–60). Candidate first Serve retention benchmark — needs corroboration before it is treated as settled. | serve-monthly-engagement-depth ad_hoc 2026-07-15 | open |
| 2026-07-15 | data | data-state | Engaged/activated-base rate is stock/flow-mixed (monthly-active numerator ÷ cumulative-ever-activated denominator); the March 2026 activation wave tripled the activated base (788→1,777) with a largely non-engaging cohort, so the April cliff read as a rate (~43%→~3%) is mostly denominator growth, not per-user engagement decay. Sharpens the existing "April 2026 cliff" gotcha. | serve-monthly-engagement-depth ad_hoc 2026-07-15 | open |
| 2026-07-15 | data | universal | Amplitude event data streams continuously — the current calendar month is open and its counts climb intraday (MAX(event_time) advanced 07-14→07-15 mid-session); the reference-MAU latest-month figure is a point-in-time snapshot. Exempt the open month from any exact-match sanity gate and label it still-filling. | serve-monthly-engagement-depth ad_hoc 2026-07-15 | open |
| 2026-07-15 | process | universal | `serve_analysis.build_serve_working_set` is single-window (one row per user over the whole post-anchor span); monthly-grain Serve analyses must re-derive the query by hand (this run did). Candidate: add a month-grain helper (e.g. `build_serve_monthly`) carrying the depth counts per user-month, which would also own the open-month gate exemption above. | serve-monthly-engagement-depth ad_hoc 2026-07-15 | parked (Track 2 OFF) |
| 2026-07-15 | process | universal | The "always report Wilson 95% CIs" convention (`methodology.md:104`, `SKILL.md:36`) is heavy-handed as a blanket rule: on large-N cuts (200–350 users/month here) CIs add noise without informing any decision; they earned their place only on the small post-April months where overlap showed the flatness was real. Candidate: make CIs conditional (report where N is small enough to be over-read, or where a difference is being claimed) rather than always-on. | serve-monthly-engagement-depth ad_hoc 2026-07-15 | promoted (PR #613, owner-approved 2026-07-15) |
| 2026-07-15 | process | universal | Framing did not stress-test the chosen metric against the falsification statement: the brief's falsification ("one-and-done vs returning", >60%) contradicted a within-month depth metric (>=2 active days) — a construct-validity gap visible on paper before any SQL, and the run's most expensive reviewer finding. Candidate: add a `framing.md` check that the outcome metric can actually move the falsification statement (metric<->falsification consistency) before approval. | serve-monthly-engagement-depth ad_hoc 2026-07-15 | promoted (PR #613, owner-approved 2026-07-15) |
| 2026-07-21 | process | universal | Framing fork-sizing on a census-count question computed the deliverable itself before the framing gate (the sizing query and the answer query are the same query), leaving the gate nothing to redirect. Candidate: framing.md coarse-sizing rule — existence/coverage/order-of-magnitude only during framing; exact per-bucket tables are execution. | q01 gold run (DATA-2144, data-2144-question-bank) | promoted (calib/process/2026-07-21-q01-gold-run; user opted into process-design mode same session) |
| 2026-07-21 | process | universal | Supervised re-run inserted an explicit brief-review pause (brief written to a file, turn ended for approval) between brief and execute; cheap, and gave the human a reviewable artifact the inline brief did not. Candidate: optional brief-file gate in pipeline.md for supervised/gold runs. | q01 gold run (DATA-2144, data-2144-question-bank) | promoted (calib/process/2026-07-21-q01-gold-run; user opted into process-design mode same session) |
| 2026-07-21 | process | universal | Stage banners embedded mid-turn (between tool calls / ahead of AskUserQuestion) were not visible to the user; only banners leading a turn's final message rendered. Candidate: pipeline.md legibility note — put banners at the head of the turn's final message, not between tool calls. | q01 gold run (DATA-2144, data-2144-question-bank) | promoted (calib/process/2026-07-21-q01-gold-run; user opted into process-design mode same session) |
| 2026-07-22 | data | data-state | engagement.md's "`Onboarding - Candidate Pledge Completed` = version-agnostic, both eras" was stale: a THIRD onboarding event generation (`Onboarding V2 - *`) was instrumented 2026-06-08 (era-2 call sites removed 06-09, last fired 06-10), so the single event is V2-era-blind — June-2026 14d completions read 96 vs 292 true. Strict completion is now the era-resolved 3-event pledge union; four docs corrected (engagement, gotchas, methodology_defaults, canonical_metrics — owner-approved). | q02 gold run (DATA-2144, data-2144-question-bank) | promoted (calib/data/2026-07-22-onboarding-v2-era; owner-approved 2026-07-22) |
| 2026-07-22 | process | universal | Reviewer dispatch cites artifacts by repo path, but HEAD switched branches mid-session (shared worktree across concurrent sessions), so a reviewer's citations pointed at files absent from the later checkout. Candidate: pin reviewer artifact reads to a commit hash, or copy repo artifacts into the session scratchpad at dispatch time. | q02 gold run (DATA-2144, data-2144-question-bank) | parked (Track 2 OFF) |
| 2026-07-22 | data | data-state | `Dashboard - Candidate Dashboard Viewed` (anchor of canonical Onboarded, Active Candidates OKR, and the activity intermediates) died in-data 2026-06-13; code-retired 2026-07-13 (omni PR #732); successor `Dashboard - Campaign Plan Viewed`. Divergence starts with May-2026 cohorts (plan-only viewers 0/0/0/0/68 Jan→May); May-2026 14d dashboard views 225 union vs 157 legacy-only; prod `is_active_candidate_30d` reads FALSE for all ~62k users (fix: DATA-2173). Cross-era dashboard metrics = 2-event union. Directed-verified (provenance CSV + per-user decomposition + prod flag check) — meets the data-state bar in one run. | q04 gold run (DATA-2144) | promoted (calib/data/2026-07-22-onboarding-v2-era; owner-approved 2026-07-22) |
| 2026-07-22 | data | data-state | May-2026 signup cohort straddles the 2026-05-07 cutover: era-2 pledge-in-14d 81.7% (179/219) vs era-1 70.5% (43/61), and May's daily signup rate (~8.8/day era-2) reversed the Jan→Apr decline — watch for composition change in cohort comparisons spanning May 2026. | q04 gold run (DATA-2144) | open |
| 2026-07-22 | data | data-state | Era accounts with no election date (6,939 as of 2026-07-22) show exactly zero activation and zero Pro — verified genuine, not a join artifact. Election-details setup acts as a hard funnel gate ahead of all activation/monetization. Candidate engagement.md funnel fact once corroborated. | win_topline_reporting project 2026-07-22 | open |
| 2026-07-22 | process | universal | Recurrence one day after promotion: the 2026-07-21 banner-placement + opt-in brief-review-pause fixes did not prevent the same failures in a standard run (banners buried mid-turn again; brief written to file, never surfaced). Owner flagged "keeps coming up, needs correcting" and hypothesized context bloat. Candidate structural fix: make the brief-review pause default-on for all runs, and move the two legibility conventions from pipeline.md into the always-loaded SKILL.md loop (attention placement over added text; net token delta ≈ 0). | win_topline_reporting project 2026-07-22 | parked (Track 2 OFF) |
| 2026-07-22 | data | data-state | Serve poll milestone can precede EO activation (min first_sms_poll_sent_at 2025-10-13 < min eo_activated_at 2025-10-23), and 11 poll-senders are not Serve users — onboarding-funnel counts need an explicit subset/ordering decision (q05 settled: strict subset of the activated population, no ordering requirement). | q05 gold run (DATA-2144, data-2144-question-bank) | open |
| 2026-07-22 | data | data-state | Serve onboarding funnel frozen: zero first-SMS-poll completions 2026-06-26 → 2026-07-22 and only ~7 external EO activations since the June-30 cutoff — extends the documented Serve engagement collapse to the onboarding funnel (also makes q05's as-of numbers unusually drift-stable). | q05 gold run (DATA-2144, data-2144-question-bank) | open |
| 2026-07-22 | data | universal | Milestone timestamps on users_serve_base (via int__amplitude_user_milestones, e.g. first_sms_poll_sent_at) are computed upstream of impersonation hygiene: a staff-impersonated poll send sets the EO's milestone. By construction; matched governed flag deliberately in q05 (caveat recorded in the key). | q05 gold run (DATA-2144, data-2144-question-bank) | open |
| 2026-07-22 | process | universal | Brief-review pause skipped, 3rd occurrence — this time in a supervised gold run where pipeline.md prescribes it; "looks good, proceed" at the framing gate was treated as waiving the pause, and the user then asked to see the brief. Corroborates the 2026-07-22 structural-fix candidate (pause default-on; conventions into always-loaded SKILL.md). Also saved to operator private memory. | q05 gold run (DATA-2144, data-2144-question-bank) | parked (Track 2 OFF) |
| 2026-07-22 | process | universal | Background reviewer subagent stalled (watchdog kill after 600s, zero output produced); relaunch with the identical dispatch prompt succeeded. Candidate: reviewer-dispatch liveness guidance (check/relaunch on stall rather than waiting open-ended). | q05 gold run (DATA-2144, data-2144-question-bank) | parked (Track 2 OFF) |
| 2026-07-23 | data | data-state | Datable-evidence bounds are subject to Amplitude ingestion lag at the pull date: 2 in-population users' elected_office.pledged_at (2026-07-22 evening) postdated the staging max event_time (2026-07-22 17:59), so "no event = pledged earlier" reasoning silently failed at the ~1-day edge. Caveat any evidence-based bound with the stream's max event_time. | q07 gold run (DATA-2144, data-2144-question-bank) | open |
| 2026-07-23 | process | universal | Mandatory-check queries must not inherit the deliverable's time filters: q07 check 2 (contamination bound) reused the numerator CTE's `pledged_at < 2026-07-01` filter, hiding the July timestamps the bound needed — reported 4, actual 6; caught by the methodology reviewer, verified and corrected pre-key. Candidate executor rule: compute uncertainty/bound checks from unfiltered sources. | q07 gold run (DATA-2144, data-2144-question-bank) | parked (Track 2 OFF) |
| 2026-07-23 | data | data-state | Poll-anchored Serve MAU (users_serve_activity) EXCEEDS broad-engagement MAU pre-cliff (Feb 2026: 363 vs 346, +4.9%; Jan: 214 vs 208) and only undercounts 2–3x from April on — the documented "undercounts the remaining base" heuristic is post-cliff-scoped; don't apply it to early-2026 months. | q08 gold run (DATA-2144, data-2144-question-bank) | open |
| 2026-07-23 | data | data-state | 2026-06 Serve onboarding revamp fired a new serve-family event wave (Magic Link / Office / Pledge / Know Your Constituents, instrumented 2026-06-18..24 per omni provenance CSV); users_serve_base funnel milestone columns are 2025-era events and may be blind to the new flow — verify int__amplitude_user_milestones coverage before any Serve onboarding-funnel analysis. | q08 gold run (DATA-2144, data-2144-question-bank) | open |
| 2026-07-23 | process | universal | Inline-deliverable runs need the exact executed SQL + pins of live dependencies (e.g. event-catalog membership hash) written into the execution record at execution time, not post-review: both q08 reviewers flagged the gap and it cost a follow-up pass. Candidate executor rule for methodology.md's verification protocol. | q08 gold run (DATA-2144, data-2144-question-bank) | parked (Track 2 OFF) |
| 2026-07-23 | data | data-state | `campaign_party` is ~46% populated, not ~95% as segmentation.md stated: 32,625/60,491 (53.9%) NULL across latest non-demo candidacies, ~45% NULL in the 2025-2026 generals voter>=1000 population. Values also fragmented across casing/format (`independent`/`Independent`, `nonpartisan`/`Non-partisan`, `green`/`Green Party`, `forward`/`Forward Party`); normalize (lowercase + strip ` Party`) before grouping. | DATA-2153 win_power_users | promoted (segmentation.md corrected 2026-07-23) |
| 2026-07-23 | data | data-state | `is_incumbent` only ~43.5% populated (56.5% NULL) in the Win 2025-2026 generals voter>=1000 population, below segmentation.md's ~70% global figure - the BR-heavy Win-generals subset is sparse. Re-verify the global ~70% figure before relying on it. | DATA-2153 win_power_users | open |
| 2026-07-23 | data | data-state | The 3-way candidate-tenure split DATA-2153 asked for (first-timer / first re-election / longer-term) is not derivable from the warehouse: only 0.43% of in-scope people have >1 candidacy in-window and `is_incumbent` is 56.5% NULL, so tenure needs the deferred BallotReady all-history pull + web-search backfill. | DATA-2153 win_power_users | open (blocked on deferred backfill) |
| 2026-07-24 | data | data-state | Cross-cycle candidacy return is ~zero in product data: of Nov-2025-period users active in 2026 (643 active / 277 engaged / 65 intense by tier), only 3/1/0 have ANY 2026 general in users_win_candidacy with scope filters off (1 user overlaps within the scoped base); the table is generals-anchored so primary-only races are invisible. Alumni usage is real but race-less on record. | DATA-2183 win_power_users | open |
| 2026-07-24 | data | data-state | Pro-datability is period-split: undatable-by-any-channel = 21% of 2026-period current Pros vs 71% of Nov-2025-period; Nov-2025 driver is absence of any Stripe record (21% match vs 67%), i.e. comped/sales-granted + billing-email mismatch, not just missing timestamps. Backend fix DATA-2185. | DATA-2183 win_power_users | promoted (PR #687, segmentation.md is_pro row; data-state bar met via 2026-07-23 topline sighting + 2026-07-24 directed per-period decomposition) |
| 2026-07-24 | process | universal | Ordering/conversion claims computed from lifetime-count columns are a live failure mode: a 48% "eventually sent" set-overlap shipped to the results checkpoint before review forced the temporal recompute (26% ordered). Candidate brief/executor rule: any pre-registered "X then Y" metric must name its timestamped ordering source at brief time. | DATA-2183 win_power_users | parked (Track 2 OFF) |
| 2026-07-24 | process | universal | Pre-registered exclusion/orthogonality variants implemented loosely pass mechanically (any-non-definitional counted co-firing generic events; the 0.9pp delta was guaranteed). Candidate brief-schema/executor rule: pin the exact exclusion set (event- or family-level) in the brief's target definition. | DATA-2183 win_power_users | parked (Track 2 OFF) |
| 2026-08-03 | data | data-state | BR filing-period / race data in the civics mart is current-cycle-only: `int__civics_election_stage_ballotready` filters `election_day >= 2026-01-01`, so 2025 races carry no `filing_period_end_on` in `election_stage`/`candidacy`. The raw BR API sources (`stg_airbyte_source__ballotready_api_election`/`_race`/`int__ballotready_filing_period`) do carry 2023-2025; use those for any historical filing-period or race lookup. | DATA-2202 win_power_users | open |
| 2026-08-03 | data | data-state | HubSpot acquisition-channel is unusable for the Win population: `lead_source` ~100% null, `utm_source` ~0.1%, `hubspot_owner_id` near-universal (non-discriminating), `hs_latest_source` all inbound/marketing with OFFLINE=8. Cannot distinguish sales-recruited vs self-serve, so the "sales outreach was off -> unvetted marketing signups" mechanism is untestable with current data. | DATA-2202 win_power_users | open |
| 2026-08-03 | data | data-state | `int__amplitude_user_milestones.dashboard_view_count` collapsed post-DATA-2173 (base p90 fell 14 -> 5 between 2026-07-22 and 2026-08-03; median now 0), so the power-user "engaged" threshold (dash_views >= base p90) is unstable across runs. Pin `dash_p90` (e.g. to the DATA-2153 value 14) for any cross-run engaged comparison. | DATA-2202 win_power_users | open |
| 2026-08-03 | data | data-state | `campaign_party` casing is a source/flow marker that correlates with external corroboration ("Independent" 57% corroborated vs lowercase "independent" 28%; lowercase third-party labels ~90% uncorroborated), so party inflates real-vs-fake classifier separability as an artifact (AUC 0.58 -> 0.67 on inclusion). Treat as a source tell, not affiliation, in any match-status model; normalize before grouping. Sharpens the promoted 2026-07-23 party row. | DATA-2202 win_power_users | open |
| 2026-08-03 | process | universal | A presence/match-based verification gate should be validated against the corroborating source's lag before it is proposed and built: the BR filing-deadline gate was designed and partly built before discovering BR's ~40d median add-lag (F5) invalidated it in-cycle. Candidate framing check: for any "is X present in source S by time T" signal, verify S's population lag vs T at framing. | DATA-2202 win_power_users | parked (Track 2 OFF) |
| 2026-08-03 | process | universal | A committed coverage/fan-out check existed in the builder (`civics_match_coverage`) but no executed script called it; the base drift (23k -> 20.9k vs the prior run) went unexplained until a reviewer flagged it. Candidate executor rule: run and report the project's committed coverage/fan-out checks as a standard execution step, not on demand. | DATA-2202 win_power_users | parked (Track 2 OFF) |
| 2026-08-03 | data | universal | `active_serve_users` is the FIRST ratified semantic-layer metric (`config.meta ratified 2026-07-28`, `sem_analytics__users_serve.yml`) but the projected `canonical_metrics.md` still read `pending` — the projection lags the yml, so check `sem_*.yml config.meta` first for governance status. Also a naming trap: `is_serve_user` (= EO activated, 1,846) vs `active_serve_users` (poll AND pledged, 1,038); "active" != "activated". | win_topline all-hands 2026-08-03 | actioned (canonical_metrics.md header callouts, PR #741; operator memory saved) |
| 2026-08-03 | data | data-state | Serve EO-activated → active gap is the SMS-poll leg: of 1,846 EO-activated Serve users (excl internal) 99.6% have pledged but only 55.7% sent an SMS poll (both 55.5% = active_serve_users). Pledging is near-universal and not the constraint; the ~44% broad-vs-ratified gap is onboarding-completion (first SMS poll). Directed-verified by decomposition this run. | win_topline all-hands 2026-08-03 | open (data-state; promote to serve knowledge on corroboration) |
| 2026-08-03 | process | universal | Framing's canonical-metric enumeration reads the projected `canonical_metrics.md`, which lags the semantic-layer yml for ratification status. Candidate: framing.md — grep `sem_*.yml config.meta` for governed/ratified status before trusting the projection. | win_topline all-hands 2026-08-03 | parked (Track 2 OFF) |
| 2026-08-04 | data | data-state | Office-declared census via the loose campaign-only join to `candidacy` overcounts vs the version-aware join (`campaign_id` + `election_date = general_election_date`): 21,210 → 20,479 users on 2025+ completed generals (731 users with campaign/candidacy election-date mismatch, ~3.5%). joins.md documents the join itself; the census-inflation consequence awaits recurrence. | DATA-2225 MVP 2026-08-04 | open |
| 2026-08-04 | data | data-state | BR-arrival-derived features are right-censored at forward-scoring time: only ~19% of the 10.5k in-cycle office-declared signups have any BR candidacies row (vs a majority of the retrospective pool); recoverable filing deadline ~51% in-cycle. Any model leaning on BR arrivals degrades toward its no-arrivals ablation in early cycle. | DATA-2225 MVP 2026-08-04 | open |
| 2026-08-04 | data | data-state | Most corroborated Win signups sign up AFTER their race's filing deadline (a before-deadline rule catches only ~43% of corroborated users); "signed up before the deadline" is directionally backwards as a legitimacy screen. Relevant to any deadline-anchored eligibility or vetting design. | DATA-2225 MVP 2026-08-04 | open |
| 2026-08-04 | process | universal | Brief pre-registered a *specific formula* heuristic baseline (`comparison_group`: non-GP domain AND not-after-deadline) that was faithfully implemented yet miscodable (fillna swept deadline-missing rows; direction inverted) — caught only at review. Candidate: briefs should specify baseline *categories* (e.g. "fair single-marker rules") and leave fair implementation to execution. | DATA-2225 MVP 2026-08-04 | parked (Track 2 OFF) |
| 2026-08-05 | data | data-state | Amplitude staging geo: `location_lat`/`location_lng` are NEVER populated (0 of 23,755 Win users' earliest geo events) and `region`/`country`/`city` exist only from 2025-04-24 onward — distance-km features are unbuildable from this source, and IP-geo features need a near-signup gate (first geo event ≤30d of signup) for PIT integrity on pre-2025-04 signups. | DATA-2225 geo features 2026-08-05 | open |
| 2026-08-05 | data | universal | Corroboration-label tautology: candidacies whose `br_normalized_position_type` is missing (BR has no position record for the declared office) can near-mechanically never corroborate (1.2% corroborated, 12% of the DATA-2225 pool, 100% nested in BR-arrivals-missing). Any corroboration-label model or analysis must treat that bucket as rule-decided, not model evidence — pooled discrimination inflates ~5 AUC points if left in. | DATA-2225 independent validation 2026-08-05 | open |
| 2026-08-06 | data | data-state | `int__ballotready_filing_period` ingestion-scope gap: BR races reference filing periods by id only (no dates inline), and the ids for races covering 16.0% of the DATA-2225 pool (3,285 users) are absent from the intermediate — vs only ~5.7% true BR gap (empty `filing_periods`). Fixing ingestion lifts pool deadline coverage 59%→~75%. Ticket 86ajx85j2 (=DATA-2235). | DATA-2225 deep-dive 2026-08-06 | actioned (ingestion widened, PR #793 `e1dbea4` + regression fix #808 `c54b5b0`, merged 2026-08-12; measured 92.2% resolve rate on a 100k-id run, 39,797 ids recovered that prod lacked. ~75% pool coverage is a hard ceiling per the ticket — ~7pp of the gap is a position+election_day join failure, not ingestion. Re-measure actual pool coverage before citing a number; 8,214 unresolved ids' cause (dead vs failed batches) is still an open follow-up) |
| 2026-08-06 | data | data-state | ~45% of post-2025-04 office-declared Win signups have ONLY server-side (session_id=-1) events in their first 30 days — zero browser presence near signup (99.8% server-side share among IP-geo-missing users vs 4.9% among covered; consistent with CRM/sales-created accounts). This, NOT the 2025-04-24 Amplitude geo-era boundary (70% of the pool post-dates it), is why IP-geo features are missing for 60% of post-era pool users; it also caps any client-side-event feature's coverage for this population. Server-side share of the whole stream swings 0-42% by month. | DATA-2225 deep-dive 2026-08-06 | open |
| 2026-08-06 | data | data-state | No-browser-presence signups (server-side-only month 1) are a 2025-wave phenomenon, not a stable feature: 60% of the post-2025-04 LABELED pool vs only 3.4% of the 2026 in-cycle scoring set (92.3% of in-cycle signups have browser presence → IP-geo coverage ~92% forward). Retrospectively no-browser predicts corroborated 72% vs 52% (86% vs 38% on completed 2026) — likely CRM/lead-import provenance, possibly BR-sourced (label-circularity risk if used as a legitimacy feature). Major 2025→2026 covariate shift for any feature correlated with it. | DATA-2225 deep-dive 2026-08-06 | open |
| 2026-08-17 | data | data-state | IP fan-out is a usable marker-free impersonation detector where the `impersonation` property is absent; device_id is NOT. On marked-era dashboard views, `ip_users_total >= 5` gives sensitivity 0.917 / specificity 0.810 (Youden J 0.727, precision only 0.324 — 15 IPs carry the top band, so it bounds prevalence but must never strip an individual user). Rejected in the same test: device_id fan-out (impersonated events average 1.0 distinct users per device — the impersonation path mints a fresh device_id) and fan-out VELOCITY (`ip_users_hour >= 5` collapses to sensitivity 0.04; reps do not cycle users fast on one IP). | DATA-2206 dashboard-viewer-leg | open |
| 2026-08-17 | data | universal | `users_win_base.total_campaigns_sent` and `is_activated` are sourced from `int__amplitude_user_milestones` (users_win_base.sql:132-134), i.e. they are Amplitude counts of `Voter Outreach - Campaign Completed`, NOT product-DB counters. So the sender leg IS impersonation-subtractable on the same marker as the viewer leg. This run asserted the opposite in a module docstring before review caught it. | DATA-2206 dashboard-viewer-leg | open |
| 2026-08-17 | data | data-state | Dashboard-view instrumentation entered a THIRD era on 2026-07-31: `Dashboard - Campaign Plan Viewed` fell 122 events/42 users (07-30) to 6/4 (07-31) and single digits since, with a 7-day hole before `Campaign Plan - Campaign Tracker Viewed` began 2026-08-07 (omni PR 1185; gp-meta records the supersession explicitly). The successor classifies to family `win_compliance_or_planning` with `is_recurrent = false`, so the 2-event union AND any family-based dashboard read both miss it. Two OKR metrics (Active Candidates, Onboarded) start reading false-low ~2026-08-31. Ticketed DATA-2337 (urgent). | DATA-2206 dashboard-viewer-leg | actioned (DATA-2337 filed 2026-08-17; row closes when the union is extended) |
| 2026-08-17 | process | universal | A frozen threshold must record which METRIC VERSION produced it. `DASH_P90_PINNED = 14.0` is p90 of the UNdeduped dashboard-view count; when the DATA-2173 30s dedup landed beneath it the same literal became ~2.8x stricter, silently shrinking the engaged tier ~2,700 to ~1,500 across DATA-2202 and DATA-2204 with no decision behind it. The threshold printed unchanged, so nothing flagged it. Sharpens the same-day DATA-2207 row about `add_derived()` recomputing `dash_p90` — promote the two together. Candidate: pinned thresholds carry the metric definition + derivation date, and re-derive when the metric changes. | DATA-2206 dashboard-viewer-leg | parked (Track 2 OFF) |
| 2026-08-17 | process | universal | A p90 rule degenerates on a tied integer grid. `>= quantile(0.90)` over-selects whenever mass piles on the percentile value: view-days p90 = 2 on a base that is 65% zero and 22% one, so the "top decile" rule selected 13.8% and would have WIDENED a cohort the run existed to tighten. Candidate: when a brief pre-registers a percentile threshold, require a check of the tie mass at that value, and prefer selectivity-matching (fix the share admitted) over percentile-matching when the metric changes. | DATA-2206 dashboard-viewer-leg | parked (Track 2 OFF) |
| 2026-08-17 | process | universal | The framing version-continuity check tests whether events existed across a product change; it does not test whether the instrument is ALIVE NOW. This run built a standing cohort definition on an event that had stopped firing 17 days earlier, and both reviewers caught it independently. The omni event-health assets answer it directly and were not consulted until review; the provenance CSV + gp-meta then named the successor in one lookup after the run had reconstructed the era empirically. Candidate: add a liveness check (last-fired date vs run date, plus gp-meta supersession) to the pre-brief checklist whenever a deliverable is a standing definition rather than a retrospective read. | DATA-2206 dashboard-viewer-leg | parked (Track 2 OFF) |
| 2026-08-17 | data | universal | Two Amplitude nav charts sit on the same dashboard over the same window with the same nine events, one metric=totals (`nri8nd9x`) and one a uniques ratio (`yllbepto`, titled "what % of active users click..."). They rank surfaces differently wherever clicks-per-user varies (voter data 5.53 vs content builder 2.65), which is what produced DATA-2207. Worth a pointer if a stakeholder cites "the nav chart" again. | DATA-2207 surface-map-nav-reconciliation | open |
| 2026-08-17 | process | universal | `win_power_users.add_derived()` recomputes `dash_p90` from the current working set when the caller passes none, so the "engaged" tier silently drifts between runs even though the comment at the threshold calls it "pinned, pre-registered". Likely part of the unexplained section 2.4 level drift (n 1,116 -> 1,045, +1.4 to +3.7pp). Candidate: require an explicit threshold, or record the resolved value in `meta`. | DATA-2207 surface-map-nav-reconciliation | parked (Track 2 OFF) |
| 2026-08-17 | process | universal | An instrument-availability guard must test that BOTH instruments cover the analysis window, not that their lifetimes intersect. This run's step-2 audit tested `overlap_days <= 0` and passed `win_resources` on a ONE-day overlap inside a 122-day window, which is what let a confounded rate reach a published recommendation. Candidate: state the covering test in the framing pre-brief checklist alongside the existing coverage-window check. | DATA-2207 surface-map-nav-reconciliation | parked (Track 2 OFF) |
| 2026-08-17 | process | universal | Scope beyond the asked question was where every wrong conclusion in this run originated: both reviewer blockers landed inside a visit-to-use conversion metric the analyst invented, and neither the ladder nor that metric was in the ask. The brief's own `target.censoring` rule would have caught one of them and was not applied to the invented measure. Candidate: a framing-stage bias toward the narrowest deliverable that answers the question, and a rule that self-added measures inherit the brief's guards explicitly. | DATA-2207 surface-map-nav-reconciliation | parked (Track 2 OFF) |
| 2026-08-18 | process | universal | A consolidation pass that updates docs but not its own timestamp is invisible to the check that schedules the next one. The 2026-08-17 pass (PR #839) edited engagement.md + gotchas.md and appended ledger rows, but left the `Last consolidation pass` header at 2026-07-21, so the 2-week nudge fired against a 4-week-stale date the very next day. Same shape as the semantic-layer ratification dates that DATA-2276 automated. Candidate: have the consolidation step write the header, or make the nudge read git history for the last consolidation-tagged commit rather than the header. | DATA-2207 close-out | parked (Track 2 OFF) |
| 2026-08-14 | data | universal | `int__civics_election_estimated_opponents` is a pure roster count (active candidacy_stage records per stage, general preferred), election-grain, one-directional (never asserts 0 opponents), and contains no viability inputs — safe under the viability-never-in-lead-scoring constraint. Promoted same day into sources.md (race-structure section) alongside the TS-only/late-arrival facts. | DATA-2204 notebook 04 | promoted (sources.md, this PR) |
| 2026-08-14 | data | data-state | The two uncontested signals disagree: 221 of 245 TS zero-opponent 2026 CC/SB races are NOT flagged by the stage-sourced `is_uncontested` (which defaults to contested when no stage record says otherwise). TS-reported zero is behaviorally validated as a dead zone (6.7-7.1% engaged). Reconcile before either is used alone. | DATA-2204 notebook 04; feeds DATA-2208 | open |
| 2026-08-14 | data | universal | The viability wrong-mart trap (read `candidacy.*` instead of `candidacy_scored`) bit again in a working-set builder that predates viability.md: `win_power_users.py` carried it from DATA-2153 until review round 2 caught it (coverage 18-30% vs 99%). Builders written before a gotcha is documented do not self-heal — audit older builders when a trap is promoted. | DATA-2204 review round 2 | open |
| 2026-08-14 | data | universal | `l2_district_type` classifies into at-large vs districted: substring ward / commissioner_district / subdistrict + {School_board_district, Board_of_education_district} = districted; whole-jurisdiction list (City, Township, Village, Borough, County, Town_district, School_district variants, Community_college) = at-large; else unknown, never read blanks as at-large. Validated by seats cross-check (districted 96% single-seat, at-large 63% multi-seat). Mapping lives in `win_power_users.classify_district_structure`. | DATA-2204 notebook 04 | open |
| 2026-08-14 | process | universal | Never record a spot-check or sample-based verdict against `head()` of an unsorted frame — the sample reshuffles on re-execution and the recorded verdict stops tracing to the printed rows (bit DATA-2204 in review round 2). Sort by a stable key (e.g. gp_candidacy_id) before sampling. | DATA-2204 review round 2 | parked (Track 2 OFF) |
